{%- set first = variants.0 -%}
# {{ theme.name }} Starship Theme

{% if adapter.mirror %}{%- set slug = theme.repository | replace(from="https://github.com/", to="") -%}
{%- set repo = adapter.mirror | split(pat="/") | last -%}
> [!IMPORTANT]
> This repository is a read-only mirror.
> Issues, pull requests, and stars should go to [{{ slug }}]({{ theme.repository }}).

{% endif -%}
Starship prompt themes for {{ theme.name }}. {{ theme.description }}

## Installation

{% if adapter.mirror -%}
Clone the repository and add the palette to your Starship config:

```bash
git clone https://github.com/{{ adapter.mirror }}.git
cat {{ repo }}/{{ theme.id }}-{{ first.variant.id }}.toml >> ~/.config/starship.toml
```
{% else -%}
Add the palette to your Starship config:

```bash
cat {{ theme.id }}-{{ first.variant.id }}.toml >> ~/.config/starship.toml
```
{% endif %}
Then set the palette in your config:

```toml
palette = '{{ theme.id }}-{{ first.variant.id }}'
```
{% for v in variants | slice(start=1) %}
Or for {{ v.variant.id }}:

{% if adapter.mirror -%}
```bash
cat {{ repo }}/{{ theme.id }}-{{ v.variant.id }}.toml >> ~/.config/starship.toml
```
{% else -%}
```bash
cat {{ theme.id }}-{{ v.variant.id }}.toml >> ~/.config/starship.toml
```
{% endif %}
```toml
palette = '{{ theme.id }}-{{ v.variant.id }}'
```
{% endfor %}
## How It Works

This overrides Starship's standard named colors, so your existing module configurations will automatically use {{ theme.name }} colors without any changes.

## Variants

{% for v in variants -%}
- **{{ theme.id }}-{{ v.variant.id }}.toml** - {{ v.variant.description }}
{% endfor -%}
